/**
 * Services index - central export for API-ready modules
 *
 * Swap implementations here when moving to real backend:
 * - authService: replace with API client (fetch/axios)
 * - cartService: add when cart needs persistence
 */

export * from "./authService";
